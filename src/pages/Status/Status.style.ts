import { styled } from "../../environments/stitches.config"

export const StatusArea = styled("main", {
  height: "100%",
  width: "100%",
  display: "flex",
  flexDirection: "column",
  paddingTop: "50px",
  overflowY: "auto",
})